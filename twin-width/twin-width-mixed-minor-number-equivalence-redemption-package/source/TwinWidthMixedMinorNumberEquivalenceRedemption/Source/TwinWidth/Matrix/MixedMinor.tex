\(\mathrm{HasMixedMinor}(M,k)\) says that \(M\) has a mixed minor of order
\(k\).  The order \(0\) case is accepted by convention.  For positive \(k\),
there must be row and column \(k\)-divisions \(R,C\) such that every cell
\((i,j)\in \mathrm{Fin}\,k\times\mathrm{Fin}\,k\) is mixed.
