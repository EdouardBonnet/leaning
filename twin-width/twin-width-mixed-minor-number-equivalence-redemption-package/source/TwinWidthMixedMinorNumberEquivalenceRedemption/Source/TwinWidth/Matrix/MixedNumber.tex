\(\mathrm{matrixMixedNumber}(M)\) is the largest order \(k\) for which \(M\)
has a mixed minor of order \(k\), searched only up to
\(\min(n,m)\) for an \(n\times m\) matrix.  The convention that every matrix
has a \(0\)-mixed minor makes the maximum defined for all finite matrices.
