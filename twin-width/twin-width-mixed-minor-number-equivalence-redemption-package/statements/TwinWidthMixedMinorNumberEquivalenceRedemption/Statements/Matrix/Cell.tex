For a matrix \(M\), a row division \(R\), and a column division \(C\), the
cell \((i,j)\) is vertical if every column of that cell is constant on the
row part \(R_i\).  It is horizontal if every row of that cell is constant on
the column part \(C_j\).
