This submission formalizes the functional equivalence between graph twin-width and graph mixed minor number for finite simple graphs.  The proof instantiates the two graph parameters by the project's concrete definitions of twin-width and mixed minor number, and combines the two formalized directional bounds.
