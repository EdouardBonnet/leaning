\(\texttt{twinWidth}(G)\) is the least natural number \(d\) such that
\(\texttt{HasContractionSequenceWidthAtMost}(G,d)\).  In Lean this least value
is implemented with \(\texttt{Nat.find}\), with fallback value \(0\) in the
empty search branch.

