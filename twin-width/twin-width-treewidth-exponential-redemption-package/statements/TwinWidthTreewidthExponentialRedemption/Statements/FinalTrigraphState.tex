\(\operatorname{FinalState}(V)\) is the subtype of trigraph states on \(V\)
whose current family of bags has cardinality at most one:
\[
  |\operatorname{bags}(T)|\le 1.
\]

For \(F:\operatorname{FinalState}(V)\), \(\operatorname{state}(F)\) is its
underlying trigraph state.
